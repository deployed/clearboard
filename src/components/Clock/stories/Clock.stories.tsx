import { Container } from 'components/Layout';

import { Clock } from '..';
export default {
    title: 'Layouts/Container',
    decorators: [],
};

export const ClockStories: React.FC = () => (
    <Clock />
         

);